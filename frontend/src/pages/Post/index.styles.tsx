import styled from "styled-components";

const MainContainer = styled.div`
  max-width: 600px;
  margin: 0 auto;
`;

const PostContainer = styled.div`
  max-width: 600px;
  margin: 0 auto;
  padding: 10px;
`;

export default {
  MainContainer,
  PostContainer,
};
