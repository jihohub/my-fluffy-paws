import styled from "styled-components";

const MainContainer = styled.div`
  max-width: 600px;
  margin: 0 auto;
  margin-bottom: 80px;
`;

const PostContainer = styled.div`
  margin: 0 auto;
`;

export default {
  MainContainer,
  PostContainer,
};
